# margin loss
use_recon = True    # use reconstruction in the loss
alpha = 0.0005      # recon scalar, 0.0005 in paper and source code
m_plus = 0.9        # presences factor
m_min = 0.1         # absence factor
use_entropy = False # use entropy in loss
beta = 0.1          # entropy scalar

# routing
bias_routing = True # source code has bias True
sparse = none

# network
stdev_W = 0.1
architecture = 32,8;10,16
